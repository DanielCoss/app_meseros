import 'package:flutter/material.dart';

class mesas extends StatelessWidget{
  const mesas({Key? key}): super(key:key);
  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text('Oprime + para agregar mas mesas'),
    );
  }
}